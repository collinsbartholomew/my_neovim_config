---
-- Databases LSP configuration
-- Note: Most databases don't have traditional LSP support,
-- but we can set up some basic completion and syntax support
local M = {}
function M.setup()
    -- Currently empty as databases don't typically use LSP
    -- This is a placeholder for future enhancements
end
return M